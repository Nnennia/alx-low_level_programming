#include "main.h"

/**
 * is_palindrome - empty string
 * @s: string
 * Return: 0 or 1
 */
int is_palindrome(char *s)
{
	if (*s == is_palindrome)
		return (1);
	else if (*s != is_palindrome)
		return (0);
}
