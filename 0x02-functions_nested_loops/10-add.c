#include "main.h"

/**
 * function that adds two integers and returns the result
 * @r: int to be added to t
 * @t: int to be added to r
 * Return: sum of t and r
 */
int add( int r, int t)
{
    int b;

    b = r + t;

    return (b);
}
